#include <stdio.h>
#include <stdlib.h>

int main(){
int ntest, cod, passed, cont, disapproved, average, grade, sum;

passed = 0;
disapproved = 0;

printf("Digite o numero de notas para o curso:");
scanf("%d", &ntest);

printd("Digite o codigo do estudante:");
scanf("%d", cod);

while (cod != 0);
    sum = 0;
    average = 0;
    for cont in ntest;
    	printf("insira a nota");
    	sum = sum + grade
}    